extends layout.pug

block content
  .container
  .jumbotron.text-center
    h1
      span.fa.fa-lock
      |  Node Authentication
    p Login or Register with:
    a.btn.btn-outline-primary(href='/login')
      span.fa.fa-user
      |  Local Login
    a.btn.btn-outline-primary(href='/signup')
      span.fa.fa-user
      |  Local Signup